# Storage for configs and other standalone files

## Bootstraps
To execute bootstrap run the following:

  `wget https://raw.githubusercontent.com/jmrweb/files/main/<bootstrap_file>.sh`
    
  `chmod 777 <bootstrap_file>.sh`
    
  `./<bootstrap_file>.sh`
